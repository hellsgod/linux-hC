pkgbase = linux-hC-rc
	pkgdesc = Linux BORE + LTO + ZEN/Cachy/Clear Tweaks + Custom set of build flags Clang 20 improved hC kernel - Release Candidate
	pkgver = 6.14.rc1
	pkgrel = 2
	url = https://github.com/hellsgod/linux-hC
	arch = x86_64
	license = GPL-2.0-only
	makedepends = bc
	makedepends = cpio
	makedepends = gettext
	makedepends = libelf
	makedepends = pahole
	makedepends = perl
	makedepends = python
	makedepends = tar
	makedepends = xz
	makedepends = zstd
	makedepends = clang
	makedepends = llvm
	makedepends = lld
	options = !strip
	options = !debug
	options = !lto
	source = https://github.com/torvalds/linux/archive/refs/tags/v6.14-rc1.tar.gz
	source = config
	source = auto-cpu-optimization.sh
	source = https://raw.githubusercontent.com/cachyos/kernel-patches/master/6.14/misc/dkms-clang.patch
	source = https://raw.githubusercontent.com/cachyos/kernel-patches/master/6.14/sched/0001-bore.patch
	source = https://raw.githubusercontent.com/graysky2/kernel_compiler_patch/refs/heads/master/more-ISA-levels-and-uarches-for-kernel-6.1.79%2B.patch
	source = 0001-linux6.14.y-le9uo-1.10.patch
	source = amd-pstate.patch
	source = amd-tlb-broadcast.patch
	source = block.patch
	source = block-Set-rq_affinity-to-force-complete-I-O-r.patch
	source = compiler-opt.patch
	source = cpuidle.patch
	source = crypto.patch
	source = fixes.patch
	source = rcu-Change-sched_setscheduler_nocheck-calls-t.patch
	source = tweaks.patch
	source = zstd.patch
	b2sums=('SKIP')

pkgname = linux-hC-rc
	pkgdesc = The Linux BORE + LTO + ZEN/Cachy/Clear Tweaks + Custom set of build flags Clang 20 improved hC kernel - Release Candidate kernel and modules
	depends = coreutils
	depends = kmod
	depends = initramfs
	optdepends = wireless-regdb: to set the correct wireless channels of your country
	optdepends = linux-firmware: firmware images needed for some devices
	optdepends = modprobed-db: Keeps track of EVERY kernel module that has ever been probed - useful for those of us who make localmodconfig
	optdepends = scx-scheds: to use sched-ext schedulers
	provides = VIRTUALBOX-GUEST-MODULES
	provides = WIREGUARD-MODULE
	provides = KSMBD-MODULE
	provides = UKSMD-BUILTIN
	provides = NTSYNC-MODULE

pkgname = linux-hC-rc-headers
	pkgdesc = Headers and scripts for building modules for the Linux BORE + LTO + ZEN/Cachy/Clear Tweaks + Custom set of build flags Clang 20 improved hC kernel - Release Candidate kernel
	depends = pahole
	depends = linux-hC-rc
	depends = clang
	depends = llvm
	depends = lld
